# Install-channel + posture e2e (MGIT-48).
#
# CI historically ran unit/integration tests from the repo checkout, where a
# dev-built mgit-sandboxd sits next to mgit and masks exactly the failures real
# users hit on a clean machine (two external trials failed on DISTRIBUTION, not
# the product). These jobs exercise what a user actually gets — an installed
# binary, no repo, both postures — and are reusable (workflow_call) so the
# release pipeline gates on them.
name: e2e

on:
  push:
    branches: [main]
    # Documentation and the board reach no e2e job — the jobs build cmd/, run
    # scripts/e2e, scripts/sandbox-image and scripts/ci, and goreleaser — so a
    # push or pull request touching only these files skips the matrix: 13 of
    # the 43 main pushes and 11 of the 41 merged PRs in the eight days to
    # 2026-09-08 were such changes. Every other path runs everything — the
    # matrix on a pull request is the evidence the merge rule reads, and no
    # leg is left to be discovered on main. Refs: MGIT-202
    paths-ignore:
      - "**/*.md"
      - "docs/**"
      - ".mtix/**"
      - "LICENSE"
      - ".gitignore"
  # Every pull request, whatever its base: a PR stacked on a feature branch
  # got no checks at all while this read `branches: [main]`, and the evidence
  # the merge rule requires lived in hand-dispatched run ids nobody running
  # `gh pr checks` would see (MGIT-200). Pushes stay filtered to main.
  pull_request:
    # The same list as push, for the same reason. Refs: MGIT-202
    paths-ignore:
      - "**/*.md"
      - "docs/**"
      - ".mtix/**"
      - "LICENSE"
      - ".gitignore"
  workflow_call: {}
  # On-demand: re-run the live containment gate against a branch without
  # waiting for a release tag. Refs: MGIT-78
  workflow_dispatch: {}
  # Daily, and hours late: GitHub creates this run well after the cron, and
  # the e2e-schedule-age status below says by how much. The fleet soak runs its
  # SHORT profile on every push and its LONG profile here: a wide fleet costs
  # minutes of real VM boots, and a gate that slow on every push gets disabled
  # rather than fixed. The soak names the profile that ran in its own output,
  # so a short run can never be read as the wide one. Refs: MGIT-113, MGIT-220
  schedule:
    - cron: "0 3 * * *"

# A push to a pull request cancels the run it supersedes: 34 of the 97 PR
# runs in the eight days to 2026-09-08 were overtaken by a newer push before
# they finished, each a full matrix nobody would read. Every other event —
# a push to main, the daily scheduled run, a release's call, a dispatch — produces
# evidence and gets a group of its own, so it is never queued behind or
# cancelled by another run (a shared group holds one pending run and cancels
# the rest). Refs: MGIT-202
concurrency:
  group: e2e-${{ github.event_name == 'pull_request' && format('pr-{0}', github.event.pull_request.number) || github.run_id }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

permissions:
  contents: read

env:
  # Pin the toolchain to the one setup-go installs, and forbid Go from
  # downloading another. Without this, go.mod's `toolchain` directive makes
  # Go fetch golang.org/toolchain AS A MODULE into the module cache; setup-go
  # then caches that directory, and the next run's restore collides with the
  # toolchain setup-go itself installed -- `gtar: Cannot open: File exists`,
  # which is a hard job failure. Keep this in step with go.mod's toolchain
  # line and the go-version pins below. Refs: MGIT-118
  GOTOOLCHAIN: local

jobs:
  # Daemon-less posture + core loop + MCP surface, on a host with NO
  # mgit-sandboxd. This is the always-on gate; it needs no virtualization.
  posture:
    name: Posture (daemon-less core loop + MCP)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: "1.26.6"

      - name: Build mgit only (no mgit-sandboxd — daemon-less posture)
        run: |
          mkdir -p "$RUNNER_TEMP/bin"
          CGO_ENABLED=0 go build -trimpath -o "$RUNNER_TEMP/bin/mgit" ./cmd/mgit/

      - name: Core-loop e2e (init/work/commit/status/squash→git apply/worktree/verify/audit)
        run: bash scripts/e2e/core_loop.sh "$RUNNER_TEMP/bin"

      - name: Course-correction e2e (rollback→fork→restore/cherry-pick→squash, append-only)
        run: bash scripts/e2e/course_correction.sh "$RUNNER_TEMP/bin"

      - name: Daemon-less posture e2e (honest wiring, run fails-closed with pointer)
        run: bash scripts/e2e/daemonless_posture.sh "$RUNNER_TEMP/bin"

      - name: REST posture e2e (real serve process, /api/v1 routes, serve+CLI lock coexistence)
        run: bash scripts/e2e/rest_posture.sh "$RUNNER_TEMP/bin"

      - name: MCP posture e2e (drive every registered tool via a real stdio client)
        env:
          MGIT_BIN: ${{ runner.temp }}/bin/mgit
        run: go run ./scripts/e2e/mcpdrive

  # Install-channel matrix: prove the documented install paths produce working
  # binaries. go install is validated on every run; the release-archive channel
  # is validated from goreleaser's own snapshot artifacts.
  install-channels:
    name: Install channel (${{ matrix.channel }})
    # The release archive's Linux daemon is the libkrun build the
    # linux-sandboxd job assembles; goreleaser takes it from there. Refs: MGIT-229
    needs: linux-sandboxd
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        channel: [go-install, release-archive]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: "1.26.6"

      - name: Install via go install
        if: matrix.channel == 'go-install'
        run: |
          bin="$RUNNER_TEMP/gobin"; mkdir -p "$bin"
          GOBIN="$bin" go install ./cmd/mgit ./cmd/mgit-sandboxd
          test -x "$bin/mgit" || { echo "mgit not installed"; exit 1; }
          test -x "$bin/mgit-sandboxd" || { echo "mgit-sandboxd not installed by go install"; exit 1; }
          echo "BINDIR=$bin" >> "$GITHUB_ENV"

      - uses: actions/download-artifact@v4
        if: matrix.channel == 'release-archive'
        with:
          name: linux-sandboxd-amd64
          path: ${{ runner.temp }}/prebuilt

      - name: Build release archive (Linux) and extract
        if: matrix.channel == 'release-archive'
        env:
          MGIT_LINUX_PREBUILT: ${{ runner.temp }}/prebuilt
        run: |
          # -t 900: the MGIT-143 rule (4x the slowest success, up the ladder)
          # applied to this step's own history -- worst success 235s, n=310.
          # The measurement is step-level (install + build + copy), so the
          # install inherits the whole step's bound rather than a split nobody
          # measured. Over-generous for a `go install` is the safe direction:
          # an over-generous bound costs minutes during a real outage, an
          # under-generous one manufactures the red it was added to prevent.
          # PINNED, not @latest. goreleaser v2.18.0 raised its Go floor to 1.27
          # and this runner is on 1.26 with GOTOOLCHAIN=local, so `@latest`
          # broke a documentation-only PR the day it was published — an input
          # nobody chose, changing under a build that must be reproducible.
          # v2.17.1 is the newest release that builds on this toolchain.
          # Bump deliberately, together with the Go version. Refs: MGIT-179
          GORELEASER_VERSION=v2.17.1
          bash scripts/ci/guard-fetch.sh -t 900 -l goreleaser-install \
            -c none:'the module cache writes each download to a temp path and renames it only after verifying the checksum, so a failed fetch leaves nothing behind for the next attempt to trip over' -- \
            go install github.com/goreleaser/goreleaser/v2@${GORELEASER_VERSION}
          # Only the Linux builds — the darwin daemon is CGO/native (built on a
          # mac in release.yml), so it is excluded from this ubuntu snapshot.
          goreleaser build --snapshot --clean --skip=before \
            --id mgit --id mgit-sandboxd-linux --single-target
          bin="$RUNNER_TEMP/archbin"; mkdir -p "$bin"
          cp "$(find dist -type f -name mgit -perm -u+x | head -1)" "$bin/mgit"
          cp "$(find dist -type f -name mgit-sandboxd -perm -u+x | head -1)" "$bin/mgit-sandboxd"
          echo "BINDIR=$bin" >> "$GITHUB_ENV"

      - name: Core-loop e2e against the installed binaries
        run: bash scripts/e2e/core_loop.sh "$BINDIR"

  # THE LINUX SANDBOX DAEMON, built where it can be built — the ONE place it
  # is built, for every pull request and for the release alike (release.yml
  # runs this workflow as its gate and ships the artifacts this job uploads
  # in that run). The Linux archives
  # ship mgit-sandboxd linked against libkrun, with libkrun and libkrunfw
  # beside it in lib/ (no Ubuntu release packages either library, and
  # firecracker refuses the sync and export the agent loop needs:
  # hyper-swe/mgit#12). It is cgo, so it cannot come from the macOS runner the
  # rest of the release is built on; each architecture is built natively here,
  # inside ubuntu:20.04 so the glibc floor is 2.31, by the same assembler the
  # e2e gate runs on every pull request. The assembler verifies its own output
  # (run paths, NET=1, glibc floor, a clean-environment load with the build
  # prefix hidden, and a negative control) before anything is uploaded.
  # Refs: MGIT-229, ADR-016
  linux-sandboxd:
    name: Linux sandbox daemon (${{ matrix.arch }}, libkrun bundled)
    runs-on: ${{ matrix.runner }}
    container:
      image: ubuntu:20.04
    strategy:
      # One architecture's failure must not hide the other's result: the
      # user-path leg needs only amd64, and an arm64-only defect (MGIT-229's
      # first run: cpio) cancelled a healthy amd64 build under fail-fast.
      fail-fast: false
      matrix:
        include:
          - arch: amd64
            runner: ubuntu-latest
          - arch: arm64
            runner: ubuntu-24.04-arm
    defaults:
      run:
        shell: bash
    steps:
      # DECLARED EXCEPTION to the MGIT-143 fetch guard, the same ordering
      # problem as the other ubuntu:20.04 jobs: checkout needs git, a bare
      # container has none, and the guard script arrives WITH the checkout.
      # Clause 2 is served by timeout-minutes; everything after checkout goes
      # through the guard (scripts/release/linux-build-prereqs.sh).
      # fetch-guard: runs before actions/checkout, so the guard script is not
      # on disk yet; clause 2 is served by timeout-minutes, clauses 1 and 3
      # are not served. Refs: MGIT-143, MGIT-144
      - name: git, so the checkout carries history
        timeout-minutes: 5
        run: |
          apt-get update -qq
          DEBIAN_FRONTEND=noninteractive apt-get install -y -qq --no-install-recommends git ca-certificates curl

      - uses: actions/checkout@v4
        with:
          # goreleaser stamps {{.ShortCommit}} and {{.CommitDate}} from a full
          # clone; the daemon must be stamped from the same history.
          fetch-depth: 0

      - name: Build prerequisites (toolchains, guarded)
        run: |
          bash scripts/release/linux-build-prereqs.sh
          echo "/usr/local/go/bin" >> "$GITHUB_PATH"
          echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"

      - name: Cache the pinned libkrunfw kernel tarball
        uses: actions/cache@v4
        with:
          path: ~/.cache/mgit-libkrun
          key: libkrunfw-kernel-${{ hashFiles('scripts/sandbox-image/pins.env') }}

      - name: Assemble and verify the daemon + its bundle
        run: |
          git config --global --add safe.directory "$GITHUB_WORKSPACE"
          # Expanded by the shell, never a literal ~ (MGIT-238).
          export MGIT_LIBKRUN_CACHE="$HOME/.cache/mgit-libkrun"
          export LIBKRUN_BUILD_DIR="$RUNNER_TEMP/libkrun-src"
          # A release stamps its tag; anything else stamps a version the
          # user-path leg tags locally, so goreleaser runs NON-snapshot there
          # and the stamp check a release relies on runs on every pull
          # request, not first on a tag that cannot be re-pointed.
          if [ "$GITHUB_REF_TYPE" = tag ]; then v="${GITHUB_REF_NAME#v}"; else v="0.0.0-ci.$GITHUB_RUN_ID"; fi
          MGIT_BUILD_VERSION="$v" \
            bash scripts/release/build-linux-sandboxd.sh "$RUNNER_TEMP/prebuilt/linux_${{ matrix.arch }}"

      # libkrunfw compiles a GPL-2.0 kernel into itself, so the release that
      # ships it publishes that kernel's corresponding source: the pinned
      # kernel tarball (digest-checked again here), libkrunfw's own tree at its
      # tag (the patches, configuration and build scripts applied to it), and
      # libkrun's. Architecture-independent, so taken from one leg.
      - name: The corresponding source (a release run only)
        if: matrix.arch == 'amd64' && github.ref_type == 'tag'
        run: |
          . scripts/sandbox-image/pins.env
          src="$RUNNER_TEMP/dist-sources"; mkdir -p "$src"
          tarball="$HOME/.cache/mgit-libkrun/linux-$LIBKRUNFW_KERNEL_VERSION.tar.xz"
          echo "$LIBKRUNFW_KERNEL_SHA256  $tarball" | sha256sum -c -
          cp "$tarball" "$src/libkrunfw-$LIBKRUNFW_VERSION-kernel-linux-$LIBKRUNFW_KERNEL_VERSION.tar.xz"
          git -C "$RUNNER_TEMP/libkrun-src/libkrunfw" archive --format=tar.gz \
            --prefix="libkrunfw-$LIBKRUNFW_VERSION/" -o "$src/libkrunfw-$LIBKRUNFW_VERSION-source.tar.gz" HEAD
          git -C "$RUNNER_TEMP/libkrun-src/libkrun" archive --format=tar.gz \
            --prefix="libkrun-$LIBKRUN_VERSION/" -o "$src/libkrun-$LIBKRUN_VERSION-source.tar.gz" HEAD
          ls -l "$src"

      - uses: actions/upload-artifact@v4
        with:
          name: linux-sandboxd-${{ matrix.arch }}
          path: ${{ runner.temp }}/prebuilt/
          if-no-files-found: error

      - uses: actions/upload-artifact@v4
        if: matrix.arch == 'amd64' && github.ref_type == 'tag'
        with:
          name: linux-sources
          path: ${{ runner.temp }}/dist-sources/
          if-no-files-found: error

  # THE DOCUMENTED USER PATH ON LINUX, as a release user meets it: the
  # release-shaped Linux layout (the archive's own goreleaser build ids, the
  # libkrun daemon the linux-sandboxd job built with its lib/ beside it,
  # guest/ beside the binaries), the one documented host requirement
  # (/dev/kvm), and NOTHING test-only. No MGIT_GUEST_* hook is set: those are
  # how the firecracker live gate below boots, and why its green says nothing
  # about this path. Compose -> launch -> exec -> sync -> export -> remove,
  # each step PASS or the exact failure. It was RED while the Linux archive
  # carried the firecracker daemon (hyper-swe/mgit#12).
  #
  # goreleaser runs NON-snapshot here, against a local tag on this run's
  # commit (never pushed) — so the build stand-in's stamp check, the one a
  # release depends on, runs on every pull request. Refs: MGIT-229
  linux-user-path:
    name: Linux user path (release-shaped, stock runner, ${{ matrix.install }})
    needs: linux-sandboxd
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        # archive: the extracted archive, lib/ beside the daemon.
        # install-script: the same archive installed by install.sh from a
        # local copy of the release, so the daemon runs from <prefix>/bin
        # with its libraries in <prefix>/lib/mgit — the README's recommended
        # path. Refs: MGIT-230.1
        install: [archive, install-script]
    steps:
      - uses: actions/checkout@v4
        with:
          # the same full history the linux-sandboxd job stamped from
          fetch-depth: 0
      - uses: actions/setup-go@v5
        with:
          go-version: "1.26.6"

      - name: Require /dev/kvm (the documented host requirement)
        run: |
          set -euo pipefail
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' \
            | sudo tee /etc/udev/rules.d/99-kvm4all.rules >/dev/null
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm || true
          ls -l /dev/kvm
          [ -r /dev/kvm ] && [ -w /dev/kvm ] \
            || { echo "FATAL: /dev/kvm not usable — the user path cannot run"; exit 1; }

      - uses: actions/download-artifact@v4
        with:
          name: linux-sandboxd-amd64
          path: ${{ runner.temp }}/prebuilt

      - name: Build the release-shaped Linux layout (goreleaser, the archive's own build ids)
        env:
          MGIT_LINUX_PREBUILT: ${{ runner.temp }}/prebuilt
        run: |
          set -euo pipefail
          # The same pin and bound as the release-archive install channel above.
          GORELEASER_VERSION=v2.17.1
          bash scripts/ci/guard-fetch.sh -t 900 -l goreleaser-install \
            -c none:'the module cache writes each download to a temp path and renames it only after verifying the checksum, so a failed fetch leaves nothing behind for the next attempt to trip over' -- \
            go install github.com/goreleaser/goreleaser/v2@${GORELEASER_VERSION}
          # A release run is already on its tag; any other run tags this
          # commit locally with the version linux-sandboxd stamped.
          if [ "$GITHUB_REF_TYPE" != tag ]; then git tag "v0.0.0-ci.$GITHUB_RUN_ID"; fi
          goreleaser build --clean --skip=before,validate \
            --id mgit --id mgit-sandboxd-linux --id mgit-guest --single-target
          bin="$RUNNER_TEMP/userbin"; mkdir -p "$bin/guest"
          mgit_bin="$(find dist -type f -path '*mgit_linux*' -name mgit -perm -u+x | head -1)"
          sbx_dir="$(dirname "$(find dist -type f -path '*mgit-sandboxd-linux_linux*' -name mgit-sandboxd | head -1)")"
          cp "$mgit_bin" "$bin/mgit"
          cp "$sbx_dir/mgit-sandboxd" "$bin/mgit-sandboxd"
          # lib/ and THIRD_PARTY/ beside the daemon, as the archive lays them out.
          cp -R "$sbx_dir/lib" "$sbx_dir/THIRD_PARTY" "$bin/"
          # guest/ as the archive lays it out: the guest runs the same CLI the host does.
          cp "$mgit_bin" "$bin/guest/mgit"
          cp "$(find dist -type f -name mgit-guest -perm -u+x | head -1)" "$bin/guest/mgit-guest"
          ls -lR "$bin"
          # The two binaries of one archive report one build.
          m="$("$bin/mgit" --version)"; d="$("$bin/mgit-sandboxd" --version)"
          echo "$m"; echo "$d"
          [ "${m#mgit version }" = "${d#mgit-sandboxd version }" ] \
            || { echo "FATAL: mgit and mgit-sandboxd report different builds"; exit 1; }

      - name: Install it the way this matrix leg says
        run: |
          set -euo pipefail
          if [ "${{ matrix.install }}" = archive ]; then
            echo "BIN=$RUNNER_TEMP/userbin" >> "$GITHUB_ENV"
            exit 0
          fi
          # A release-shaped tarball and checksums.txt from the laid-out
          # archive, installed by the repository's install.sh as a user runs
          # it, from a local copy of the release instead of GitHub.
          if [ "$GITHUB_REF_TYPE" = tag ]; then ver="$GITHUB_REF_NAME"; else ver="v0.0.0-ci.$GITHUB_RUN_ID"; fi
          rel="$RUNNER_TEMP/release"; mkdir -p "$rel"
          name="mgit_${ver#v}_linux_amd64.tar.gz"
          tar -czf "$rel/$name" -C "$RUNNER_TEMP/userbin" .
          (cd "$rel" && sha256sum "$name" > checksums.txt)
          MGIT_VERSION="$ver" MGIT_DOWNLOAD_BASE="file://$rel" MGIT_PREFIX="$RUNNER_TEMP/prefix" sh install.sh
          find "$RUNNER_TEMP/prefix" -type f | sort
          echo "BIN=$RUNNER_TEMP/prefix/bin" >> "$GITHUB_ENV"

      - name: The bundle loads on this host (no libkrun installed here)
        run: bash scripts/release/verify-linux-sandboxd.sh "$BIN"

      - name: The user path — must PASS
        run: bash scripts/e2e/linux_user_path.sh "$BIN"

  # LIVE Linux containment gate: real firecracker microVMs on real KVM.
  #
  # This job used to be "Sandbox posture (gated; skips without virtualization)"
  # and asserted the opposite of what is true: it stated GitHub-hosted runners
  # have no nested virtualization, built no mgit-sandboxd, and therefore only
  # ever exercised sandbox_posture.sh's SKIP branch — a green check that proved
  # nothing about containment. Measured 2026-08-10 on ubuntu-latest,
  # ubuntu-24.04 and ubuntu-22.04: /dev/kvm is present (root:kvm 0660, so the
  # udev rule below is what makes it usable by the `runner` user), KVM_CREATE_VM
  # succeeds, and the full firecracker battery boots and passes here. The
  # headline claim of the product is now gated by CI on every push, PR and
  # release instead of by the availability of one physical box. Refs: MGIT-78
  #
  # It fails LOUD in both directions that matter: a runner that stops offering
  # /dev/kvm fails the job rather than skipping, and a posture SKIP is treated
  # as a failure — a skip that reads as a pass is the exact defect this repo
  # keeps finding (the macOS libkrun gate, 2026-08-05).
  sandbox-live-linux:
    name: Sandbox LIVE (Linux/KVM firecracker)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: "1.26.6"

      - name: Require /dev/kvm (nested virtualization)
        run: |
          set -euo pipefail
          # /dev/kvm ships root:kvm 0660 and `runner` is not in the kvm group;
          # the static_node rule is what actually grants access.
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' \
            | sudo tee /etc/udev/rules.d/99-kvm4all.rules >/dev/null
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm || true
          ls -l /dev/kvm
          # Assert, do not skip: if the hosted runner class ever loses nested
          # virt, this gate must go red so the loss is noticed, not silently
          # degrade back into an unrun manual checklist item.
          [ -r /dev/kvm ] && [ -w /dev/kvm ] \
            || { echo "FATAL: /dev/kvm not usable — the Linux live gate cannot run"; exit 1; }

      - name: Install the pinned firecracker VMM
        run: |
          sudo bash scripts/sandbox-image/fetch-firecracker.sh amd64 /usr/local/bin/firecracker

      # THE STEP THAT EARNED CLAUSE 2. On 2026-08-18 (run 32096641338) this sat
      # for 21592s -- 5h59m52s -- and was ended not by anything here but by the
      # job's own six-hour ceiling. Its median is 7s (n=134); its worst
      # SUCCESSFUL run is 214s. apt returned no error the whole time, which is
      # why retry alone would not have helped: there was nothing to retry until
      # something declared failure. A re-run cleared it in 3m11s, twice.
      #
      # -t 900 is the MGIT-143 rule -- 4x the 214s worst success, up the ladder
      # -- so a hang is a failure in 15 minutes instead of six hours, and the
      # two further attempts are the ones the incident record says would have
      # cleared it. The restore is real: a killed apt leaves a locked dpkg
      # front-end and half-written package lists, and the next attempt inherits
      # them. Refs: MGIT-143, MGIT-119
      - name: Host tooling (mke2fs for worktree delivery, iptables for egress)
        run: |
          apt_restore='sudo rm -rf /var/lib/apt/lists/* /var/cache/apt/archives/partial/*; sudo dpkg --configure -a'
          bash scripts/ci/guard-fetch.sh -t 900 -l apt-update -c "$apt_restore" -- \
            sudo apt-get update -qq
          bash scripts/ci/guard-fetch.sh -t 900 -l apt-install-host-tooling -c "$apt_restore" -- \
            sudo apt-get install -y -qq e2fsprogs iproute2 iptables

      - name: Build mgit + mgit-sandboxd (no -tags libkrun — Linux links firecracker)
        run: |
          mkdir -p "$RUNNER_TEMP/bin"
          CGO_ENABLED=0 go build -trimpath -o "$RUNNER_TEMP/bin/mgit" ./cmd/mgit/
          CGO_ENABLED=0 go build -trimpath -o "$RUNNER_TEMP/bin/mgit-sandboxd" ./cmd/mgit-sandboxd/

      - name: Build the guest image (pinned kernel + reproducible rootfs)
        run: |
          mkdir -p "$RUNNER_TEMP/guest"
          bash scripts/sandbox-image/fetch-kernel-fc.sh amd64 "$RUNNER_TEMP/guest/vmlinux"
          bash scripts/sandbox-image/build-rootfs.sh   amd64 "$RUNNER_TEMP/guest/rootfs.ext4"

      - name: Sandbox posture e2e — must PASS (live), a SKIP fails the job
        run: |
          set -uo pipefail
          # shellcheck source=scripts/sandbox-image/pins.env
          . scripts/sandbox-image/pins.env
          export PATH="$RUNNER_TEMP/bin:$PATH"
          export MGIT_GUEST_KERNEL="$RUNNER_TEMP/guest/vmlinux"
          export MGIT_GUEST_ROOTFS="$RUNNER_TEMP/guest/rootfs.ext4"
          # Without a cmdline the registered image boots a kernel with no root=
          # and no init=, and the only symptom is "guest vsock not ready".
          export MGIT_GUEST_CMDLINE="$FC_CMDLINE"
          bash scripts/e2e/sandbox_posture.sh "$RUNNER_TEMP/bin" 2>&1 | tee "$RUNNER_TEMP/posture.log"
          grep -q 'SANDBOX POSTURE E2E: PASS (live)' "$RUNNER_TEMP/posture.log" || {
            echo "FATAL: the posture gate did not pass LIVE (a SKIP is a failure here)"; exit 1; }
          # MGIT-191: the harness's daemon-leak check must be shown to FIRE, or it proves nothing.
          bash scripts/e2e/sandbox_posture_negative_control.sh "$RUNNER_TEMP/bin"

      # The sandbox CLI verbs an operator actually types. They were unit-only
      # until MGIT-88: unit tests exercise the service BENEATH the CLI, and this
      # project's defects keep landing in the gap between the two (MGIT-77,
      # MGIT-83, MGIT-65 were each green at the unit layer and broken for a
      # user). `status`, `list` and `remove` are what someone reaches for first
      # when a sandbox misbehaves, so a defect there surfaces at the worst
      # possible moment. Refs: MGIT-88
      - name: Sandbox CLI surface e2e (list/status/exec/shell/grants/remove)
        env:
          MGIT_GUEST_KERNEL: ${{ runner.temp }}/guest/vmlinux
          MGIT_GUEST_ROOTFS: ${{ runner.temp }}/guest/rootfs.ext4
        run: |
          set -uo pipefail
          # shellcheck source=scripts/sandbox-image/pins.env
          . scripts/sandbox-image/pins.env   # defines FC_CMDLINE; without it
          # `set -u` kills this step on an unbound variable before a single verb
          # runs, which is how this gate sat dead while reporting failure for
          # days. The posture step above already sources it; this one did not.
          export PATH="$RUNNER_TEMP/bin:$PATH"
          export MGIT_GUEST_CMDLINE="$FC_CMDLINE"
          bash scripts/e2e/sandbox_cli_surface.sh "$RUNNER_TEMP/bin" 2>&1 | tee "$RUNNER_TEMP/cli.log"
          grep -q 'SANDBOX CLI SURFACE E2E: PASS (live)' "$RUNNER_TEMP/cli.log" || {
            echo "FATAL: the CLI surface gate did not pass LIVE (a SKIP is a failure here)"; exit 1; }
          # `grants` rides the daemon's egress escalation stack, which is
          # Linux-only, so on THIS gate it must actually run.
          grep -q 'grants runs and reports' "$RUNNER_TEMP/cli.log" || {
            echo "FATAL: grants did not run on the Linux/KVM gate, where the egress stack is wired"
            exit 1; }
          # `shell` is deliberately NOT asserted here. It is unavailable in
          # EVERY build -- sandboxd.Client.Shell returns
          # ErrShellTransportUnavailable unconditionally, with no platform check
          # and no build tag. The earlier assertion demanded it run on Linux,
          # inferred from the error text "requires the KVM guest PTY transport";
          # that string describes what the transport WOULD need, not something
          # any build serves, and the gate has been red on that ever since.
          # Capability inferred from a message rather than verified in the code
          # is the same mistake this repo keeps paying for. Whether the
          # transport should be served is MGIT-94; until it is, there is
          # nothing here to assert.

      # Registry durability across a daemon restart. A CONTINUOUS gate by
      # explicit requirement, not a one-time proof: the live registry used to be
      # daemon-process memory, so a `mgit work --sandbox` registration vanished
      # when its daemon idle-exited (the intended NFR-17.6 behavior) and
      # `mgit sandbox status` answered "sandbox not found" for containment an
      # agent had been told it had. That is loss of containment AVAILABILITY,
      # which pushes an agent under progress pressure toward bare host commands.
      # A store unit test proves rows persist; only this proves a real CLI finds
      # a real sandbox after the daemon that registered it is gone.
      # "This class shouldn't be findable twice." Refs: MGIT-102
      - name: Sandbox registry durability e2e (survives a daemon restart)
        env:
          MGIT_GUEST_KERNEL: ${{ runner.temp }}/guest/vmlinux
          MGIT_GUEST_ROOTFS: ${{ runner.temp }}/guest/rootfs.ext4
        run: |
          set -uo pipefail
          # shellcheck source=scripts/sandbox-image/pins.env
          . scripts/sandbox-image/pins.env   # defines FC_CMDLINE; without it
          # `set -u` kills the step before a single verb runs.
          export PATH="$RUNNER_TEMP/bin:$PATH"
          export MGIT_GUEST_CMDLINE="$FC_CMDLINE"
          bash scripts/e2e/sandbox_registry_durability.sh "$RUNNER_TEMP/bin" 2>&1 |
            tee "$RUNNER_TEMP/registry.log"
          grep -q 'SANDBOX REGISTRY DURABILITY E2E: PASS (live)' "$RUNNER_TEMP/registry.log" || {
            echo "FATAL: the registry durability gate did not pass LIVE (a SKIP is a failure here)"; exit 1; }
          # This job HAS a guest image, so the booted-sandbox reconciliation arm
          # must actually run here. A "NOT EXERCISED" note means the live phase
          # was skipped and the gate proved less than its headline says.
          if grep -q 'NOT EXERCISED' "$RUNNER_TEMP/registry.log"; then
            echo "FATAL: the booted-sandbox reconciliation arm did not run on a job that has a guest image"
            exit 1
          fi

      # Fleet soak + chaos. Everything the registry/reaping class was fixed for
      # was verified SEQUENTIALLY, one sandbox at a time; MGIT-98's aggregate
      # refusal was proven by unit test alone because reaching it needs a real
      # concurrent fleet. This drives N sandboxes through create/exec/destroy
      # with genuine overlap, SIGKILLs the daemon with execs in flight, and
      # asserts six invariants -- reaping, registry honesty, capacity floor,
      # state-dir isolation, audit-trail coherence, and a clean ceiling refusal.
      # Short profile here (every push); the daily scheduled run runs the wide one.
      # Refs: MGIT-113, MGIT-98, MGIT-102, MGIT-103
      - name: Fleet soak + chaos gate (concurrent sandboxes under churn)
        env:
          MGIT_GUEST_KERNEL: ${{ runner.temp }}/guest/vmlinux
          MGIT_GUEST_ROOTFS: ${{ runner.temp }}/guest/rootfs.ext4
          MGIT_SOAK_PROFILE: ${{ github.event_name == 'schedule' && 'long' || 'short' }}
        run: |
          set -uo pipefail
          # shellcheck source=scripts/sandbox-image/pins.env
          . scripts/sandbox-image/pins.env   # defines FC_CMDLINE; without it
          # `set -u` kills the step before a single sandbox is created.
          export PATH="$RUNNER_TEMP/bin:$PATH"
          export MGIT_GUEST_CMDLINE="$FC_CMDLINE"
          bash scripts/e2e/sandbox_fleet_soak.sh "$RUNNER_TEMP/bin" 2>&1 |
            tee "$RUNNER_TEMP/soak.log"
          grep -q 'SANDBOX FLEET SOAK: PASS' "$RUNNER_TEMP/soak.log" || {
            echo "FATAL: the fleet soak did not pass (a SKIP is a failure here)"; exit 1; }
          # Say what ran. The soak names its own profile, and this asserts the
          # one that ran is the one this event asked for -- otherwise a scheduled run
          # could quietly report a short run's result as the wide soak's.
          grep -q "$MGIT_SOAK_PROFILE profile" "$RUNNER_TEMP/soak.log" || {
            echo "FATAL: expected the $MGIT_SOAK_PROFILE profile; the soak reports otherwise"
            exit 1; }
          # Known defects are filed, unfixed violations the soak deliberately
          # does not fail on -- they would block every push on a defect that is
          # already tracked. They must stay VISIBLE: a run that silently carried
          # them would be the decoration this gate exists to avoid.
          if grep -q 'PASS WITH KNOWN DEFECTS' "$RUNNER_TEMP/soak.log"; then
            echo "::warning::fleet soak passed with KNOWN DEFECTS — invariants violated by filed, unfixed tickets:"
            grep 'KNOWN DEFECT' "$RUNNER_TEMP/soak.log" || true
          fi

      - name: Firecracker e2e — unprivileged half
        env:
          MGIT_TEST_KERNEL: ${{ runner.temp }}/guest/vmlinux
          MGIT_TEST_ROOTFS: ${{ runner.temp }}/guest/rootfs.ext4
          MGIT_E2E_GUEST_ROOTFS: ${{ runner.temp }}/guest/rootfs.ext4
        run: |
          set -uo pipefail
          go test ./internal/sandboxd/backend/firecracker/ -run 'TestE2E_' \
            -v -count=1 -timeout 30m 2>&1 | tee "$RUNNER_TEMP/fc-user.log"
          grep -q '^ok  ' "$RUNNER_TEMP/fc-user.log" || { echo "FATAL: firecracker e2e failed"; exit 1; }
          # The identity proof is meaningful in THIS half, where the daemon's
          # identity is the unprivileged runner user: the guest must switch to
          # it and the worktree image must be writable by it. Asserted by name
          # — a SKIP would leave the claim unproven. Refs: MGIT-151
          grep -q -- '--- PASS: TestE2E_Exec_RunsAsTheIdentityAsked' "$RUNNER_TEMP/fc-user.log" \
            || { echo "FATAL: TestE2E_Exec_RunsAsTheIdentityAsked did not PASS in the unprivileged half"; exit 1; }

      # The root half is where containment is actually proven: tap + iptables
      # REDIRECT (CAP_NET_ADMIN) is how this backend enforces egress, and the
      # MGIT-78 live-policy pair lives here.
      - name: Firecracker e2e — root-gated half (network, port publish, live policy)
        run: |
          set -uo pipefail
          # The guest probe is cross-compiled OUTSIDE sudo and handed over, so
          # the privileged run needs no toolchain of its own.
          mkdir -p "$RUNNER_TEMP/probe"
          (cd internal/sandboxd/backend/libkrun/testdata/netprobe &&
            GOOS=linux GOARCH=amd64 CGO_ENABLED=0 go build -trimpath -buildvcs=false \
              -o "$RUNNER_TEMP/probe/netprobe" .)
          go test -c -o "$RUNNER_TEMP/fc.test" ./internal/sandboxd/backend/firecracker/
          sudo -E env PATH="$PATH" \
            MGIT_TEST_KERNEL="$RUNNER_TEMP/guest/vmlinux" \
            MGIT_TEST_ROOTFS="$RUNNER_TEMP/guest/rootfs.ext4" \
            MGIT_E2E_GUEST_ROOTFS="$RUNNER_TEMP/guest/rootfs.ext4" \
            MGIT_E2E_NETPROBE="$RUNNER_TEMP/probe/netprobe" \
            "$RUNNER_TEMP/fc.test" -test.run 'TestE2E_' -test.v -test.count=1 \
            -test.timeout 30m 2>&1 | tee "$RUNNER_TEMP/fc-root.log"
          grep -q '^PASS$' "$RUNNER_TEMP/fc-root.log" || { echo "FATAL: root-gated firecracker e2e failed"; exit 1; }

      # The guest supervisor's credential switch is proven where a process may
      # make it: as root, without a VM. Asserted positively — a SKIP here
      # means the test did not run, and it is the only host-side proof that a
      # command asked to run unprivileged does. Refs: MGIT-151
      - name: Guest supervisor identity switch (root, no VM) — must PASS, a SKIP fails the job
        run: |
          set -uo pipefail
          sudo -E env PATH="$PATH" go test ./internal/guest/ -run 'TestExecute_AsRoot_SwitchesToTheAskedIdentity' \
            -v -count=1 2>&1 | tee "$RUNNER_TEMP/guest-identity.log"
          grep -q -- '--- PASS: TestExecute_AsRoot_SwitchesToTheAskedIdentity' "$RUNNER_TEMP/guest-identity.log" \
            || { echo "FATAL: the identity switch proof did not PASS (skipped or failed)"; exit 1; }
      - name: Assert the root half SKIPPED nothing
        run: |
          set -uo pipefail
          # With KVM + root + a guest image, every gate in this package is
          # satisfied, so a SKIP means a prerequisite silently went missing and
          # the suite quietly stopped proving anything.
          if grep -n -- '--- SKIP' "$RUNNER_TEMP/fc-root.log"; then
            echo "FATAL: a firecracker e2e skipped under root — the gate did not run"; exit 1
          fi
          echo "no skips in the root-gated battery"

      - name: Assert the live-policy proofs RAN and passed
        run: |
          set -uo pipefail
          # Named explicitly rather than trusted to the package result: the
          # MGIT-78 pair is the whole reason that ticket exists, the MGIT-156
          # proof is the only live evidence that a policy staged onto an
          # unbooted sandbox is what its guest enforces, and their gates
          # (requireGuestImage + requireNetRoot) skip silently.
          for t in TestE2E_Firecracker_Revoke_KillsEstablishedFlow \
                   TestE2E_Firecracker_Revoke_DrainKeepsEstablishedFlow \
                   TestE2E_Firecracker_StagedPolicy_CreatedSandboxBootsEnforcingIt; do
            grep -q -- "--- PASS: $t " "$RUNNER_TEMP/fc-root.log" \
              || { echo "FATAL: $t did not PASS (skipped or failed)"; exit 1; }
          done
          echo "== live-policy evidence (MGIT-78, MGIT-156) =="
          grep -E 'revoke applied|drained revoke applied|hold probe|post-revoke flow refused|REAL VM PASS|STAGED POLICY' \
            "$RUNNER_TEMP/fc-root.log" || true

  # LIVE Linux containment gate for the OTHER backend: real libkrun microVMs on
  # real KVM.
  #
  # WHY IT EXISTS. Linux had exactly one validated backend, firecracker, and
  # firecracker cannot re-stage into a running guest (`sandbox sync`) or read a
  # tree out of one (`sandbox export`) — it delivers the worktree as a
  # launch-time ext4 image. An agent loop that edits the host worktree between
  # rounds therefore had NO validated option on Linux at all: the backend that
  # can do it, libkrun, had never booted in validation on this platform. That
  # gap would have been discovered at a deployment. This job is the answer, and
  # it is a GATE rather than a one-time report precisely so a regression fails
  # here instead of at someone's install. Refs: MGIT-87, MGIT-86, MGIT-78
  #
  # HOW IT DIFFERS FROM sandbox-live-linux ABOVE, in the two ways that matter:
  #   - libkrun is LINKED, not exec'd, so the daemon is a cgo build carrying
  #     `-tags libkrun` and the library must be built from source first. No
  #     distro packages it and there is no Linux tap.
  #   - it runs in a `container:` with /dev/kvm passed through. Measured
  #     2026-08-11 (run 31482480288): inside `ubuntu:20.04` with
  #     `options: --device /dev/kvm` the device is present and, because a job
  #     container runs as root, already usable — KVM_GET_API_VERSION=12 and
  #     KVM_CREATE_VM succeeds, with no udev rule involved. The MGIT-78 rule is
  #     a HOST-side fix and cannot help a container, since host steps cannot run
  #     before the job's own container starts. The pinned 20.04 toolchain is
  #     what the libkrun build recipe was validated against.
  sandbox-live-linux-libkrun:
    name: Sandbox LIVE (Linux/KVM libkrun)
    runs-on: ubuntu-latest
    container:
      image: ubuntu:20.04
      options: --device /dev/kvm
    defaults:
      run:
        # The container's /bin/sh is dash, which has no `set -o pipefail` —
        # and every assertion below depends on a pipeline's exit status.
        shell: bash
    steps:
      # DECLARED EXCEPTION to the MGIT-143 fetch guard — the same ordering
      # problem as ci.yml's libkrun-linux job: this runs BEFORE
      # actions/checkout, so scripts/ci/guard-fetch.sh is not on disk yet, and
      # moving checkout first would push it onto the REST tarball path because
      # a bare ubuntu:20.04 has no git (this step is what installs it).
      # MGIT-144 carries that. Clause 2 is served natively below: 5 minutes is
      # 4x this step's worst SUCCESSFUL run (56s, n=256) up the ladder.
      # fetch-guard: runs before actions/checkout, so the guard script is not
      # on disk yet; clause 2 is served by timeout-minutes below, clauses 1
      # and 3 are not served. Refs: MGIT-143, MGIT-144
      - name: Install build prerequisites
        timeout-minutes: 5
        run: |
          apt-get update -qq
          DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends \
            build-essential flex bison libelf-dev python3-pyelftools bc \
            pkg-config curl git ca-certificates patchelf \
            libclang1-18 libclang-18-dev libclang-common-18-dev libllvm18

      - uses: actions/checkout@v4

      # -t 120 is the floor (worst success 5s, n=246). The restore is the
      # load-bearing half: a `curl: (18) transfer closed` leaves a TRUNCATED
      # /tmp/go.tgz that the next attempt would re-extract and fail on
      # identically. Refs: MGIT-143 clause 3
      - name: Install Go
        run: |
          bash scripts/ci/guard-fetch.sh -t 120 -l go-toolchain-tarball \
            -c 'rm -f /tmp/go.tgz' -- \
            curl -fsSL https://go.dev/dl/go1.26.6.linux-amd64.tar.gz -o /tmp/go.tgz
          tar -C /usr/local -xzf /tmp/go.tgz
          echo "/usr/local/go/bin" >> "$GITHUB_PATH"

      - name: Install Rust (distro rustc is far too old for libkrun)
        # The musl target is required, not decorative: krun-init-blob's build.rs
        # links krun-init as a static musl binary and panics without it.
        #
        # Three fetches, three guards. -t 120 is the floor; worst success 26s
        # (n=246). Refs: MGIT-143
        run: |
          bash scripts/ci/guard-fetch.sh -t 120 -l rustup-installer \
            -c 'rm -f /tmp/rustup.sh' -- \
            curl -sSf https://sh.rustup.rs -o /tmp/rustup.sh
          bash scripts/ci/guard-fetch.sh -t 120 -l rustup-toolchain \
            -c 'rm -rf "$HOME/.rustup" "$HOME/.cargo"' -- \
            sh /tmp/rustup.sh -y --default-toolchain stable
          echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"
          bash scripts/ci/guard-fetch.sh -t 120 -l rustup-musl-target \
            -c none:'rustup unpacks a component into a temp directory and moves it into place only once the download is complete and hash-checked; a failed target add leaves the toolchain exactly as it was' -- \
            "$HOME/.cargo/bin/rustup" target add "$(uname -m)-unknown-linux-musl"

      - name: Require /dev/kvm (passed into this container, not granted by udev)
        run: |
          set -euo pipefail
          ls -l /dev/kvm
          # Assert, never skip: if the runner class or the `--device` passthrough
          # ever stops working, this gate must go red so the loss is noticed
          # rather than silently degrading into an unrun checklist item.
          [ -r /dev/kvm ] && [ -w /dev/kvm ] \
            || { echo "FATAL: /dev/kvm not usable — the Linux libkrun gate cannot run"; exit 1; }

      # The pinned kernel tarball, cached across runs.
      #
      # libkrunfw downloads ~145 MB from a third party before it compiles
      # anything, on every run. A transfer to a rented runner was therefore on
      # the critical path of the whole repository, and a network hiccup there
      # blocked an unrelated PR three times in a row. The tarball is PINNED, so
      # its identity is its content, so it is cacheable by construction; the
      # build verifies its digest whether the bytes came from the cache or the
      # network, so this can only make the run faster, never weaker.
      # Refs: MGIT-163, MGIT-147
      - name: Cache the pinned libkrunfw kernel tarball
        uses: actions/cache@v4
        with:
          path: ~/.cache/mgit-libkrun
          key: libkrunfw-kernel-${{ hashFiles('scripts/sandbox-image/pins.env') }}

      - name: Build the PINNED libkrunfw + libkrun (NET=1)
        env:
          MGIT_LIBKRUN_CACHE: ~/.cache/mgit-libkrun
        run: bash scripts/sandbox-image/build-libkrun.sh /usr/local

      - name: Build mgit + the libkrun-linked mgit-sandboxd
        env:
          PKG_CONFIG_PATH: /usr/local/lib64/pkgconfig
          LIBRARY_PATH: /usr/local/lib64
          LD_LIBRARY_PATH: /usr/local/lib64
        run: |
          set -euo pipefail
          git config --global --add safe.directory "$GITHUB_WORKSPACE" || true
          mkdir -p "$RUNNER_TEMP/bin"
          CGO_ENABLED=0 go build -buildvcs=false -trimpath -o "$RUNNER_TEMP/bin/mgit" ./cmd/mgit/
          # The daemon is the half that links libkrun: cgo ON, tag ON.
          CGO_ENABLED=1 go build -buildvcs=false -trimpath -tags libkrun \
            -o "$RUNNER_TEMP/bin/mgit-sandboxd" ./cmd/mgit-sandboxd/
          # The guest pair goes where a RELEASE INSTALL puts it — beside the
          # binary in guest/ — rather than being left to `sandbox base from`'s
          # source-checkout fallback. That fallback cannot work here anyway
          # (the posture script runs in a scratch dir, so `go build ./cmd/...`
          # has no module), and using the install layout means this gate
          # exercises the lookup a user actually gets. Refs: MGIT-65
          mkdir -p "$RUNNER_TEMP/bin/guest"
          CGO_ENABLED=0 go build -buildvcs=false -trimpath -o "$RUNNER_TEMP/bin/guest/mgit" ./cmd/mgit/
          CGO_ENABLED=0 go build -buildvcs=false -trimpath -o "$RUNNER_TEMP/bin/guest/mgit-guest" ./cmd/mgit-guest/

      - name: Sandbox posture e2e — launch, exec, land — must PASS (live)
        env:
          PKG_CONFIG_PATH: /usr/local/lib64/pkgconfig
          LIBRARY_PATH: /usr/local/lib64
          LD_LIBRARY_PATH: /usr/local/lib64
          # libkrun needs no kernel or rootfs of its own: libkrunfw supplies the
          # kernel and the guest base is a DIRECTORY composed from an OCI image —
          # the one this release records (internal/sandboxd/guestbase/
          # release-base.json), composed by digest, so the base this leg tests
          # is the base the release vouches for. The variable is the guard's
          # input: on Linux the posture script cannot tell which backend the
          # daemon links without one, and a SKIP here is a failure. (MGIT-219)
          MGIT_GUEST_RELEASE_BASE: "1"
        # This is the SAME shared script sandbox-live-linux requires of
        # firecracker, and it is a requirement again rather than a tripwire:
        # until MGIT-91 the first `mgit run` after a launch was reset by the
        # VMM, so this could not pass here at all. A SKIP is a failure.
        run: |
          set +e -uo pipefail
          export PATH="$RUNNER_TEMP/bin:$PATH"
          bash scripts/e2e/sandbox_posture.sh "$RUNNER_TEMP/bin" 2>&1 \
            | tee "$RUNNER_TEMP/posture-libkrun.log"
          grep -q 'SANDBOX POSTURE E2E: PASS (live)' "$RUNNER_TEMP/posture-libkrun.log" || {
            echo "FATAL: the libkrun posture gate did not pass LIVE (a SKIP is a failure here)"; exit 1; }
          # MGIT-191: the harness's daemon-leak check must be shown to FIRE, or it proves nothing.
          bash scripts/e2e/sandbox_posture_negative_control.sh "$RUNNER_TEMP/bin"

      - name: The VALIDATED column — boot, exec, sync, export, SEC-03 containment
        env:
          PKG_CONFIG_PATH: /usr/local/lib64/pkgconfig
          LIBRARY_PATH: /usr/local/lib64
          LD_LIBRARY_PATH: /usr/local/lib64
          MGIT_E2E_LIBKRUN: "1"
        run: |
          set -uo pipefail
          # The set is named in scripts/e2e/libkrun_linux_column.sh, which is
          # also what the capability tables in README.md and
          # docs/INSTALL-SANDBOX.md are written from.
          go test -buildvcs=false -tags libkrun ./internal/sandboxd/backend/libkrun/ \
            -run "$(bash scripts/e2e/libkrun_linux_column.sh --validated-pattern)" \
            -v -count=1 -timeout 30m 2>&1 | tee "$RUNNER_TEMP/libkrun-validated.log"
          grep -q '^ok  ' "$RUNNER_TEMP/libkrun-validated.log" \
            || { echo "FATAL: the validated Linux/libkrun column did not pass"; exit 1; }

      - name: The INTERMITTENT set — run and report, gated neither way
        env:
          PKG_CONFIG_PATH: /usr/local/lib64/pkgconfig
          LIBRARY_PATH: /usr/local/lib64
          LD_LIBRARY_PATH: /usr/local/lib64
          MGIT_E2E_LIBKRUN: "1"
        run: |
          set +e -uo pipefail
          # The guest exec channel resets sometimes on this backend and not
          # others (MGIT-91) — the same test passed in three environments and
          # then failed here with no code change. Gating on a pass would make
          # this job flaky; gating on a failure would entrench a defect that
          # usually does not fire. So it RUNS, its outcome is printed for the
          # flake record, and nothing depends on it — which is also why the
          # capability tables do not claim guest exec on Linux/libkrun.
          pattern="$(bash scripts/e2e/libkrun_linux_column.sh --flaky-pattern)"
          if [ -z "$pattern" ]; then
            echo "no intermittent tests: the list is empty (MGIT-91 closed the last one)"
            exit 0
          fi
          go test -buildvcs=false -tags libkrun ./internal/sandboxd/backend/libkrun/ \
            -run "$pattern" -v -count=1 -timeout 15m 2>&1 | tee "$RUNNER_TEMP/libkrun-flaky.log"
          echo "== intermittent-set outcome (informational) =="
          grep -E -- '^--- (PASS|FAIL|SKIP)' "$RUNNER_TEMP/libkrun-flaky.log" || true

      - name: The KNOWN-GAP tripwire — these must still fail, and say why
        env:
          PKG_CONFIG_PATH: /usr/local/lib64/pkgconfig
          LIBRARY_PATH: /usr/local/lib64
          LD_LIBRARY_PATH: /usr/local/lib64
          MGIT_E2E_LIBKRUN: "1"
        run: |
          set -uo pipefail
          # Deliberately NOT `|| exit 1`: these are expected to fail today, and
          # the assertion below is that they still do. A gap that closes without
          # anyone noticing leaves the shipped tables understating the backend,
          # which is how a deployment ends up relying on something unvalidated.
          pattern="$(bash scripts/e2e/libkrun_linux_column.sh --gap-pattern)"
          if [ -z "$pattern" ]; then
            echo "no known gaps remain: the tripwire list is empty"
            : > "$RUNNER_TEMP/libkrun-gap.log"
            exit 0
          fi
          go test -buildvcs=false -tags libkrun ./internal/sandboxd/backend/libkrun/ \
            -run "$pattern" -v -count=1 -timeout 15m 2>&1 | tee "$RUNNER_TEMP/libkrun-gap.log" || true

      - name: Assert the declared column — every capability, every gap
        run: |
          bash scripts/e2e/libkrun_linux_column.sh \
            "$RUNNER_TEMP/libkrun-validated.log" "$RUNNER_TEMP/libkrun-gap.log"
          echo "== Linux/libkrun live evidence =="
          grep -E 'REAL VM PASS' "$RUNNER_TEMP/libkrun-validated.log" || true

  schedule-age:
    # A LATE PASS LOOKS EXACTLY LIKE A PROMPT ONE. Since 2026-08-27 this
    # workflow's scheduled run has been created about five hours after its
    # cron — 25 days measured, the cause outside anything we can see — and
    # nobody noticed, precisely because it stays green: the run is green, the
    # list is green, and a reader between the cron and the run is reading
    # YESTERDAY's result believing it is today's. So the scheduled run
    # publishes its result WITH its timing, on the commit it tested, where
    # the green is read.
    #
    # THE JOB'S CONCLUSION IS NOT THE VERDICT — THE STATUS TEXT IS, and the
    # two must not be able to disagree. A reporting job that always exits 0
    # shows a green conclusion while its own description reads NOT green;
    # that shape cost a sibling lane an hour today, reading `completed/
    # success` three times while the text said DRIFT. So this job exits with
    # its own state. It can only say NOT green when one of the jobs it needs
    # was already red, so the run was failing anyway and no new red is
    # invented here.
    #
    # HOW TO READ THIS STATUS, in three cases rather than two, because the
    # obvious rule ("a missing status is not a pass") would block every merge
    # here: it is posted ONLY by a scheduled run, so on a push commit it is
    # ABSENT — not a pass, not a red, no information at all. Present and
    # success is the scheduled run's green together with its measured age. Present and
    # failure means one of the four jobs it needs was already red; present and
    # error means the instrument could not do the one thing it exists for.
    # Refs: MGIT-220, MGIT-174
    name: Scheduled-run age
    if: ${{ always() && github.event_name == 'schedule' }}
    needs: [posture, install-channels, sandbox-live-linux, sandbox-live-linux-libkrun]
    runs-on: ubuntu-latest
    permissions:
      statuses: write
      actions: read # to read this run's own run_started_at; statuses:write alone cannot
    steps:
      - uses: actions/checkout@v4
      - name: publish this run's result together with its timing
        env:
          GH_TOKEN: ${{ github.token }}
          POSTURE: ${{ needs.posture.result }}
          INSTALL: ${{ needs.install-channels.result }}
          FIRECRACKER: ${{ needs.sandbox-live-linux.result }}
          LIBKRUN: ${{ needs.sandbox-live-linux-libkrun.result }}
        run: |
          set -u
          # The cron is READ FROM THIS FILE, never restated here: a second
          # copy of the hour drifts from the schedule it claims to measure,
          # and that drift would be invisible — the very failure this job ends.
          cron=$(grep -o 'cron: *"[^"]*"' .github/workflows/e2e.yml | head -1 | sed 's/.*"\(.*\)"/\1/')
          min=${cron%% *}; rest=${cron#* }; hour=${rest%% *}
          # "0 3 * * *" is 03:00Z, not 3:0Z — a time a reader has to decode is
          # a time they will misread. A non-numeric field (a step or a range)
          # is printed as written rather than forced into two digits.
          pad() { case "$1" in '' | *[!0-9]*) printf '%s' "$1" ;; *) printf '%02d' "$1" ;; esac; }
          hh=$(pad "$hour"); mm=$(pad "$min")
          # THIS JOB RUNS LAST, so its own clock is the run's END. The lateness
          # being reported is the SCHEDULING lateness, so it is measured from
          # the run's own start; using `now` would silently add however long
          # the run took (median 0.20h here, once 6.00h). Unreadable is said
          # out loud rather than quietly replaced by a wrong number.
          # A NON-EMPTY CAPTURE IS NOT A VALUE. On a failed request gh writes
          # the ERROR BODY to stdout and does not apply --jq — measured on gh
          # 2.88.1 against this repo, a 404 prints {"message":"Not Found",…}
          # and exits 1 — so the previous form stored that JSON as the
          # timestamp, stopped retrying, failed to parse it, counted the empty
          # parse as zero and published a nonsense delay as a PASS. The value
          # is taken only if gh exited 0 AND the result is an instant `date`
          # accepts. Retried, because a transient read must not red an
          # otherwise green night; a persistent one must, and does below.
          started_epoch=""
          for attempt in 1 2 3; do
            if raw=$(gh api "repos/$GITHUB_REPOSITORY/actions/runs/$GITHUB_RUN_ID" --jq .run_started_at 2>/dev/null) &&
              [ -n "$raw" ] && started_epoch=$(date -u -d "$raw" +%s 2>/dev/null); then
              break
            fi
            started_epoch=""
            sleep 5
          done
          now=$(date -u +%s)
          due=$(date -u -d "today ${hh}:${mm}" +%s)
          [ "$now" -lt "$due" ] && due=$((due - 86400))
          if [ -n "$started_epoch" ]; then
            tenths=$(( (started_epoch - due) * 10 / 3600 ))
            timing="started $(date -u -d "@$started_epoch" +%H:%MZ) (+$((tenths / 10)).$((tenths % 10))h)"
          else
            timing="started UNREADABLE (this run's metadata could not be read; actions:read?)"
          fi
          bad=""
          for j in "posture=$POSTURE" "install=$INSTALL" "firecracker=$FIRECRACKER" "libkrun=$LIBKRUN"; do
            case "${j#*=}" in success | skipped) ;; *) bad="$bad ${j%%=*}:${j#*=}" ;; esac
          done
          # A timing this job could not read is NOT a pass: the instrument
          # failed at the one thing it exists to do, and `error` says that
          # without claiming the jobs were bad. A red job outranks it.
          state=success; verdict="all jobs passed"
          [ -z "$started_epoch" ] && state=error
          if [ -n "$bad" ]; then state=failure; verdict="NOT green —$bad"; fi
          # Absolute instants, not an age: an age computed here is wrong for
          # every reader after this second. They can subtract.
          desc="$verdict; cron ${hh}:${mm}Z → $timing, published $(date -u -d "@$now" +%H:%MZ)"
          printf 'e2e-schedule-age: %s\n' "$desc"
          { echo '## Scheduled-run age'; echo; echo "$desc"; } >> "$GITHUB_STEP_SUMMARY"
          gh api -X POST "repos/$GITHUB_REPOSITORY/statuses/$GITHUB_SHA" \
            -f state="$state" -f context=e2e-schedule-age \
            -f description="$(printf '%s' "$desc" | cut -c1-139)" \
            -f target_url="$GITHUB_SERVER_URL/$GITHUB_REPOSITORY/actions/runs/$GITHUB_RUN_ID" >/dev/null ||
            { echo "could not publish the status"; exit 1; }
          [ "$state" = success ] || exit 1
